apiVersion: v1
kind: Service
metadata:
  name: axon
  labels:
    application: comic-gator
    service: axon
    role: api
  namespace: default 
spec:
  ports:
    - port: 80
      protocol: TCP
      targetPort: 9000
  selector:
    service: axon
  type: NodePort
